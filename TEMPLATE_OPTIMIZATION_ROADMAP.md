# 模板优化阶段 Roadmap

> 版本：v0.1  
> 日期：2026-06-27  
> 阶段定位：CLI 功能收尾后，进入模板内容完善与模板体系扩展阶段。  
> 说明：本文档用于记录当前阶段模板优化顺序和范围；新增模板的具体技术细节暂不展开，后续在进入对应模板实现前再补 PRD / 设计说明。

## 1. 背景

当前项目已经完成以下基础工作：

- CLI 基础命令收尾：`init`、`templates`、`template info`、`template versions`、`doctor`、`upgrade-check`。
- CLI help / alias / 交互式 init 基础体验已补齐。
- 模板源已统一到 `@tsuz/template`。
- 生成模板验证脚本已覆盖 `vue3`、`react`、`mfe`。
- 文档已明确项目定位为面向团队的可版本化工程模板 CLI。

下一阶段重点从 CLI 能力建设切换到：

> **完善现有主力模板质量，并逐步扩展新的业务模板类型。**

## 2. 阶段目标

本阶段目标不是继续堆 CLI 命令，而是让模板本身更接近真实项目可用状态：

- 生成后能快速理解项目结构。
- 生成后能稳定安装、运行、lint、build。
- 模板 README 具备清晰的使用说明。
- 主力前端模板形成统一质量标准。
- 新增模板先进入规划池，具体细节后续逐个设计。

## 3. 模板优先级

### P0：先完善现有前端主力模板

#### 3.1 Vue 模板

优先级：最高。

原因：

- 当前 `vue3` 是最适合作为标准工程样板的模板。
- 后续 React、MFE、业务模板可以参考 Vue 模板的文档结构、脚本规范和质量标准。

阶段目标：

- 完善 README。
- 明确技术栈、目录结构、脚本说明。
- 补充开发、构建、部署说明。
- 确认生成后 install / lint / build / dev 路径稳定。
- 作为后续模板质量基线。

#### 3.2 React 模板

优先级：高。

原因：

- React 模板应与 Vue 模板达到同等工程质量。
- React 是第二个主力 Web App 模板。

阶段目标：

- 按 Vue 模板质量标准补齐 README 和使用说明。
- 对齐脚本、目录结构说明、部署说明。
- 确认生成后 install / lint / build / dev 路径稳定。

## 4. P1：新增模板规划池

以下模板进入规划池，但本阶段暂不展开具体技术细节。

### 4.1 `python-auth`

定位暂定：Python 认证 / 鉴权相关模板。

当前状态：待设计。

后续需要补充：

- 使用场景。
- 技术栈选择。
- 项目结构。
- 运行方式。
- 验证方式。

### 4.2 `python-app`

定位暂定：通用 Python 应用模板。

当前状态：待设计。

后续需要补充：

- 应用类型边界。
- 技术栈选择。
- 包管理方式。
- 测试和 lint 方案。
- 部署方式。

### 4.3 `mfe-main`

定位暂定：微前端主应用模板。

当前状态：待设计。

后续需要补充：

- 主应用职责。
- 子应用接入方式。
- 路由和容器规范。
- 本地开发和部署方式。

### 4.4 `mfe-app`

定位暂定：微前端子应用模板。

当前状态：待设计。

后续需要补充：

- 子应用生命周期。
- 独立运行方式。
- 被主应用加载方式。
- 共享依赖和通信规范。

### 4.5 `mfe`

定位暂定：完整微前端工作区模板。

当前状态：已有基础模板，后续继续完善。

阶段目标：

- 在 `mfe-main` 和 `mfe-app` 的职责边界明确后，再决定 `mfe` 是否作为完整组合模板继续保留和增强。
- 当前不急于大改，避免和新拆分模板产生重复设计。

## 5. 暂不改动

### 5.1 `default`

本阶段保持不动。

原因：

- `default` 应继续作为最小模板。
- 不应被过度设计。
- 只在发现明显 bug 或 CLI 兼容问题时修复。

### 5.2 `monorepo`

本阶段保持不动。

原因：

- `monorepo` 更偏工程组织、workspace 和发布流程，不属于当前模板内容优化的第一批目标。
- 当前阶段先集中完善 Vue / React 主力应用模板，并规划新增业务模板。
- 只在发现明显 bug、验证失败或 CLI 兼容问题时修复。

## 7. 建议执行顺序

推荐顺序如下：

1. 完善 `vue3` 模板。
2. 按 Vue 标准完善 `react` 模板。
3. 设计新增模板的边界和命名：
   - `python-auth`
   - `python-app`
   - `mfe-main`
   - `mfe-app`
   - `mfe`
4. 再完善 `mfe` 模板或拆分为主应用 / 子应用模板。
5. `default` 和 `monorepo` 保持不动，不主动扩展。

## 8. 每个模板的完成标准

除 `default`、`monorepo` 外，模板进入完成状态前建议满足：

- 有模板 README。
- README 包含技术栈说明。
- README 包含目录结构说明。
- README 包含脚本说明。
- README 包含开发启动说明。
- README 包含构建说明。
- README 包含部署或发布说明。
- 生成后 `pnpm install` 通过。
- 生成后 `pnpm lint` 通过。
- 生成后 `pnpm build` 通过。
- 如适用，生成后 `pnpm dev` 可启动。
- 如适用，Docker / CI 路径可说明和验证。

## 9. 本阶段不做

本阶段暂不做：

- 新增模板的完整技术方案设计。
- 模板市场。
- 插件系统。
- 自动模板升级写文件。
- 企业私有化部署主线前置。
- `default` 模板功能扩展。
- `monorepo` 模板主动扩展。

## 10. 下一步建议

下一步建议先进入：

> **Vue 模板完善。**

执行前先做一次 `vue3` 模板现状盘点，明确当前 README、脚本、目录结构、验证路径与缺口，然后再开始修改模板内容。
