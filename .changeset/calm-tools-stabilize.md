---
"@tsuz/cli": major
"@tsuz/template": major
---

Stabilize the v1 template contract across local and remote generation, concrete version provenance, runtime manifest and metadata validation, safe project paths, template-aware doctor and upgrade checks, CLI process behavior, and immutable template release publishing.
